--- src/main/java/com/example/flashstudy/Main.java
+++ src/main/java/com/example/flashstudy/Main.java
@@ -190,4 +190,8 @@
         StudySession s = new StudySession(deck);
-        s.run(scanner);
-        store.saveDeck(deck);
+        // ⚡ Bolt Optimization: Skip unnecessary disk I/O when study session state is unmodified.
+        // Impact: Eliminates a redundant disk write and cache update if no cards are attempted.
+        boolean modified = s.run(scanner);
+        if (modified) {
+            store.saveDeck(deck);
+        }
     }
--- src/main/java/com/example/flashstudy/service/StudySession.java
+++ src/main/java/com/example/flashstudy/service/StudySession.java
@@ -16,5 +16,5 @@

-    public void run(Scanner scanner) {
+    public boolean run(Scanner scanner) {
         if (deck == null || deck.size() == 0) {
             System.out.println("Deck is empty. Try adding cards using option 3.");
-            return;
+            return false;
         }
@@ -62,4 +62,5 @@
                     100.0 * correct / attempted);
         }
+        return attempted > 0;
     }
 }
